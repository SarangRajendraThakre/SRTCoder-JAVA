
import java.util.ArrayList;

public class srt {
    
    public static int lss(int arr[])
    {int n = arr.length;
        if(n == 0)
        {
            return 0;
        }
        ArrayList<Integer> s = new ArrayList();
        for(int num:arr)
        {
          int index=binarysearch(s,num);
          if(index=s.size())
          {
            s.add(num);
          }
          else{
            s.set(index, num);+91 80808 75103
          }
        }
        ret
}
