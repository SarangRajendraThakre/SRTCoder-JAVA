## ⚙️ Application and Server Configuration

# Application Name
spring.application.name=MVC_Web_2

# Server Port Configuration (You can change this if 8080 is blocked)
# Since you were running on 8080 by default, let's keep it clean.
# If you uncomment this, restart and use /sarang/welcome:
 server.port=8081
# Context path used in your last log. Must be included in the URL:
#server.servlet.context-path=/sarang 

## 🌐 View Resolver Configuration for JSP

# CRUCIAL: JSP Prefix (matches the src/main/webapp/WEB-INF/view/ folder)
spring.mvc.view.prefix=/webapp/WEB-INF/view/ 

# CRUCIAL: JSP Suffix
spring.mvc.view.suffix=.jsp

## 🗃️ Database (Oracle) Configuration

spring.datasource.url=jdbc:oracle:thin:@//localhost:1521/SCOTT
spring.datasource.username=SCOTT
spring.datasource.password=TIGER
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

## 🛠️ Hibernate and JPA Configuration

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Optional: Disable Open-in-View warning
spring.jpa.open-in-view=false