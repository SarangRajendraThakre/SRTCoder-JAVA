
public class P8_P_SmallestOfTwoNumbers {

    public static void main(String[] args) {
        // Your code here

        // Write a program to find the smallest of two numbers.

        int n = 32;
        int m = 433;
        // if (n > m) {
        // System.out.println(m + " is smaller then " + n);
        // } else {
        // System.out.println(n + " is smaller then " + m);
        // }

    String  ans =     n < m ? n + " is smaller than " + m : m + " is smaller then " + n;
    System.out.println(ans);

        System.out.println(n < m ? n + " is smaller than " + m : m + " is smaller then " + n);

    }
}
